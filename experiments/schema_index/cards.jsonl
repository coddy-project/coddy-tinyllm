{"path": "agent", "section": "agent", "type": "object", "description": "Defaults for the main agent loop (model id and safety caps)."}
{"path": "agent.llm_first_token_timeout_ms", "section": "agent", "type": "integer", "description": "How long a streamed LLM call may stay silent before the turn cancels it (the API hang guard). An explicit 0 disables the guard; blocking (stream: false) transports are never guarded.", "default": 90000, "minimum": 0}
{"path": "agent.llm_min_interval_ms", "section": "agent", "type": "integer", "description": "Minimum gap between consecutive LLM calls in milliseconds, retry attempts included (0 disables pacing; e.g. 12000 on strict free tiers).", "default": 0, "minimum": 0}
{"path": "agent.llm_retry_base_ms", "section": "agent", "type": "integer", "description": "Initial backoff between LLM retries, in milliseconds. A server-provided pause (Retry-After-Ms / Retry-After headers, \"Limit resets at\" / \"retry in Ns\" body phrases) overrides the exponential backoff, capped at 60s.", "default": 1000, "minimum": 0}
{"path": "agent.llm_retry_max", "section": "agent", "type": "integer", "description": "Retries after retryable LLM errors such as HTTP 429. An explicit 0 disables retries.", "default": 3, "minimum": 0}
{"path": "agent.loop_guard", "section": "agent", "type": "boolean", "description": "Runaway-loop protection: cut a streamed response that degenerates into repeating itself, and block a tool called over and over with identical arguments.", "default": true}
{"path": "agent.loop_nudge_max", "section": "agent", "type": "integer", "description": "How many times one turn may be nudged back on track before the loop guard stops it with a notice.", "default": 2, "minimum": 0}
{"path": "agent.loop_stream_repeat_cycles", "section": "agent", "type": "integer", "description": "Identical back-to-back output cycles inside one streamed response before the stream is cut. 0 disables the stream check.", "default": 5, "minimum": 0}
{"path": "agent.loop_tool_repeat_limit", "section": "agent", "type": "integer", "description": "Consecutive identical tool calls (same name, same canonical arguments) before the loop guard steps in. 0 disables the tool-repeat check.", "default": 3, "minimum": 0}
{"path": "agent.max_tokens_per_turn", "section": "agent", "type": "integer", "description": "Max tokens across all LLM calls in one turn.", "default": 200000, "minimum": 0}
{"path": "agent.max_turns", "section": "agent", "type": "integer", "description": "Hard cap on LLM calls per prompt turn.", "default": 30, "minimum": 0}
{"path": "agent.model", "section": "agent", "type": "string", "description": "Default models[].model id used until the client overrides the model per session. Required when models is non-empty."}
{"path": "agent.wait_for_limit_reset", "section": "agent", "type": "boolean", "description": "Wait for a hit usage limit to lift and re-issue the call, instead of ending the turn with the provider's error. Applies to a top-level turn whose provider names a pause beyond the retry budget (a 429 with Retry-After or \"Limit resets at\"); the turn lock and the client stream stay open while it waits, so it is off by default.", "default": false}
{"path": "agent.wait_for_limit_reset_max_ms", "section": "agent", "type": "integer", "description": "Longest time one turn spends waiting for limits in total, in milliseconds, the retry wrapper's own sleeps on a limit included; a pause that would exceed it ends the turn at once with the error. An explicit 0 never waits.", "default": 14400000, "minimum": 0}
{"path": "compaction", "section": "compaction", "type": "object", "description": "Summarizes older conversation history so long sessions keep fitting the model context window. Manual compact command plus automatic trigger at a percent of the model's max_context_tokens."}
{"path": "compaction.enable", "section": "compaction", "type": "boolean", "description": "Master switch for compaction (manual command and automatic trigger). Defaults to true.", "default": true}
{"path": "compaction.keep_recent_turns", "section": "compaction", "type": "integer", "description": "How many most recent user turns (each with the agent replies and tool activity after it) stay verbatim; only history before that boundary is summarized. 0 summarizes the whole window.", "default": 2, "minimum": 0}
{"path": "compaction.model", "section": "compaction", "type": "string", "description": "Optional models[].model used for the summarization call. Empty uses the session's effective model.", "default": ""}
{"path": "compaction.result_eviction", "section": "compaction", "type": "object", "description": "Collapses superseded read/grep tool results to short placeholders when building the LLM request (the persisted transcript is never rewritten), so paging a large file or a wide search cannot pin dead lines in every later turn. Only results the model marks (keep_result, or keep:true) or the most recent working window survive; a write to a file invalidates earlier reads/greps that covered it."}
{"path": "compaction.result_eviction.enable", "section": "compaction", "type": "boolean", "description": "Master switch for read/grep result eviction. Defaults to true.", "default": true}
{"path": "compaction.result_eviction.keep_recent", "section": "compaction", "type": "integer", "description": "How many most recent evictable results (read pages, grep dumps) stay intact as a working window. 0 keeps none. The default of 2 keeps a read and a grep live at the same time; with 1, a model comparing two results keeps re-fetching whichever the other evicted.", "default": 2, "minimum": 0}
{"path": "compaction.result_eviction.min_result_bytes", "section": "compaction", "type": "integer", "description": "Results at or below this size are never evicted (too small to be worth a placeholder). 0 makes every result a candidate.", "default": 2000, "minimum": 0}
{"path": "compaction.threshold_percent", "section": "compaction", "type": "integer", "description": "Auto-compaction fires when the estimated context usage reaches this percent of the effective model's max_context_tokens (1..100). Models without max_context_tokens skip auto-compaction; the manual command still works.", "default": 80, "minimum": 1, "maximum": 100}
{"path": "gateways", "section": "gateways", "type": "object", "description": "Messenger bot adapters (used only by binaries built with -tags gateway or -tags gateway.telegram; started by coddy serve alongside every other enabled subsystem). See https://coddy.dev/docs/surfaces/gateway."}
{"path": "gateways.telegram", "section": "gateways", "type": "object", "description": "Telegram bot adapter."}
{"path": "gateways.telegram.admins", "section": "gateways", "type": "array<integer>", "description": "Telegram user IDs with elevated rights; admins always pass access checks."}
{"path": "gateways.telegram.chats", "section": "gateways", "type": "array<object>", "description": "Per-chat overrides; chat_id is negative for groups and supergroups."}
{"path": "gateways.telegram.chats.[].access", "section": "gateways", "type": "string", "description": "Per-chat access override: \"all\", \"admins\", or \"group:<name>\"."}
{"path": "gateways.telegram.chats.[].chat_id", "section": "gateways", "type": "integer", "description": "Telegram chat id (negative for groups/supergroups)."}
{"path": "gateways.telegram.chats.[].isolation", "section": "gateways", "type": "string", "description": "Per-chat session isolation override.", "enum": ["individual", "shared", "admin"]}
{"path": "gateways.telegram.default_access", "section": "gateways", "type": "string", "description": "Fallback access level for chats without an override: \"all\", \"admins\", or \"group:<name>\".", "default": "all"}
{"path": "gateways.telegram.default_isolation", "section": "gateways", "type": "string", "description": "Fallback session isolation for group chats: \"individual\" (session per user), \"shared\" (one session for all), \"admin\" (admins only, shared session).", "enum": ["individual", "shared", "admin"], "default": "individual"}
{"path": "gateways.telegram.enable", "section": "gateways", "type": "boolean", "description": "Poll Telegram in this coddy serve process.", "default": false}
{"path": "gateways.telegram.proxy", "section": "gateways", "type": "string", "description": "Optional outbound proxy for Telegram API requests: http, https, socks5, or socks5h URL.", "default": ""}
{"path": "gateways.telegram.rich_messages", "section": "gateways", "type": "boolean", "description": "Use Bot API 10.1 Rich Messages (native Markdown, streamed thinking placeholder, collapsible tool list). Falls back to legacy formatting when unsupported.", "default": false}
{"path": "gateways.telegram.token", "section": "gateways", "type": "string", "description": "Bot token from @BotFather. Leave empty to read the TELEGRAM_BOT_TOKEN environment variable (e.g. via ~/.coddy/.env).", "default": ""}
{"path": "gateways.telegram.user_groups", "section": "gateways", "type": "array<object>", "description": "Named sets of user IDs referenced from access fields as group:<name>."}
{"path": "gateways.telegram.user_groups.[].name", "section": "gateways", "type": "string", "description": "Group name referenced as group:<name>."}
{"path": "gateways.telegram.user_groups.[].user_ids", "section": "gateways", "type": "array<integer>", "description": "Telegram numeric user IDs in this group."}
{"path": "hooks", "section": "hooks", "type": "object", "description": "Operator commands run at lifecycle points of a session: before and after a tool call, when a prompt is submitted, when the agent stops, on session start and around compaction. Definitions are JSON files in the Claude Code shape (~/.coddy/hooks.json, .coddy/hooks.json, .claude/settings*.json). See https://coddy.dev/docs/features/hooks."}
{"path": "hooks.default_timeout_seconds", "section": "hooks", "type": "integer", "description": "Hard limit for one hook process whose definition gives no timeout. 0 uses the default.", "default": 60}
{"path": "hooks.enable", "section": "hooks", "type": "boolean", "description": "Load and run hooks at all.", "default": true}
{"path": "hooks.files", "section": "hooks", "type": "array<string>", "description": "Definition files, lowest priority first; every matching hook runs. ${CODDY_HOME} and ${CWD} expand; a relative entry resolves against the session cwd. Files inside the workspace are project scope and follow project_trust; only the hooks key of a Claude Code settings file is read.", "default": ["${CODDY_HOME}/hooks.json", "${CWD}/.claude/settings.json", "${CWD}/.claude/settings.local.json", "${CWD}/.coddy/hooks.json"]}
{"path": "hooks.max_output_chars", "section": "hooks", "type": "integer", "description": "Cap on the context, messages and reasons one hook may hand to the model or the user; longer values are truncated with a marker. 0 uses the default.", "default": 10000}
{"path": "hooks.project_trust", "section": "hooks", "type": "string", "description": "Trust policy for hook files found inside the workspace, which travel with the checkout: \"ask\" lists them but runs nothing until the operator approved that exact file for that workspace on the machine running coddy (coddy hooks trust there, or POST /coddy/hooks/trust with the session workspace as cwd); \"allow\" treats them like the operator's own file; \"deny\" never reads them.", "enum": ["ask", "allow", "deny"], "default": "ask"}
{"path": "hooks.stop_loop_limit", "section": "hooks", "type": "integer", "description": "How many times per turn a Stop hook may send the agent back to work. 0 uses the default.", "default": 5}
{"path": "httpserver", "section": "httpserver", "type": "object", "description": "OpenAI-compatible HTTP API defaults (used only by binaries built with -tags http; the embedded SPA needs -tags http,ui). See https://coddy.dev/docs/reference/http-api."}
{"path": "httpserver.allow_insecure", "section": "httpserver", "type": "boolean", "description": "Silence the startup warning about a non-loopback bind without authentication.", "default": false}
{"path": "httpserver.auth_token", "section": "httpserver", "type": "string", "description": "Optional bearer credential for the HTTP API. Empty means no authentication (historical default). Use a \"${ENV}\" reference to keep the secret out of the file; never echoed back by GET /coddy/config. Prefer --auth-token / CODDY_HTTP_TOKEN to keep it out of config.yaml entirely. See https://coddy.dev/docs/operate/remote.", "default": ""}
{"path": "httpserver.cors", "section": "httpserver", "type": "object", "description": "Cross-origin access so a browser UI on another origin can call this API (e.g. the bundled UI pointed at a remote server). Bearer auth still applies."}
{"path": "httpserver.cors.allowed_origins", "section": "httpserver", "type": "array<string>", "description": "Exact origins permitted to call the API, e.g. \"http://localhost:5173\". A single \"*\" allows any origin.", "default": []}
{"path": "httpserver.cors.enable", "section": "httpserver", "type": "boolean", "description": "Handle CORS preflight and emit Access-Control-* headers for allowed origins.", "default": false}
{"path": "httpserver.enable", "section": "httpserver", "type": "boolean", "description": "Serve the HTTP API (and the embedded SPA) in this process. Omitted means true; set false on a node that only polls a messenger or relays a swarm.", "default": true}
{"path": "httpserver.host", "section": "httpserver", "type": "string", "description": "Bind address for the HTTP API. Empty means 127.0.0.1; set 0.0.0.0 to accept connections from other machines.", "default": "127.0.0.1"}
{"path": "httpserver.login", "section": "httpserver", "type": "object", "description": "Optional password sign-in for the browser, so a server on a network is not readable by everyone who finds the port. Off unless an account exists here or in CODDY_HTTP_USER / CODDY_HTTP_PASSWORD. API clients keep using auth_token. See https://coddy.dev/docs/operate/remote."}
{"path": "httpserver.login.enable", "section": "httpserver", "type": "boolean", "description": "Turn the sign-in form on or off explicitly. Omitted follows the credentials: an account here or in the environment enables it. false keeps the form off with the variables still set.", "default": null}
{"path": "httpserver.login.mode", "section": "httpserver", "type": "string", "description": "How a browser authenticates. Only \"password\" is implemented; the key exists so a trusted-proxy mode can be added without moving your configuration.", "enum": ["password"], "default": "password"}
{"path": "httpserver.login.password_hash", "section": "httpserver", "type": "string", "description": "argon2id hash of the password in PHC form (\"$argon2id$v=19$m=...$...\"), written by `coddy serve set-password`. Never echoed back by GET /coddy/config, and a save from the settings screen preserves it. Put a plaintext password in CODDY_HTTP_PASSWORD instead of here.", "default": ""}
{"path": "httpserver.login.session_ttl_hours", "section": "httpserver", "type": "integer", "description": "How long a browser stays signed in. 0 means the cookie is dropped when the browser closes; the server still expires the session itself after 30 days, because a record it keeps forever is not a session.", "default": 0, "minimum": 0}
{"path": "httpserver.login.user", "section": "httpserver", "type": "string", "description": "Account name for the sign-in form. Supports \"${ENV}\" references, e.g. \"${CODDY_HTTP_USER}\".", "default": ""}
{"path": "httpserver.port", "section": "httpserver", "type": "integer", "description": "Default listen port when coddy serve does not pass -P/--port. 0 falls back to 12345.", "default": 0, "minimum": 0, "maximum": 65535}
{"path": "httpserver.public_docs", "section": "httpserver", "type": "boolean", "description": "When auth is enabled, keep /docs and /openapi.* reachable without a token.", "default": false}
{"path": "httpserver.remotes", "section": "httpserver", "type": "array<object>", "description": "Remote coddy serve servers offered in the UI environment selector. Tokens are not stored here; the UI keeps them client-side per remote.", "default": []}
{"path": "httpserver.remotes.[].name", "section": "httpserver", "type": "string", "description": "Display label for the remote."}
{"path": "httpserver.remotes.[].url", "section": "httpserver", "type": "string", "description": "Base URL of the remote coddy serve server, e.g. \"https://box.example:12345\"."}
{"path": "instructions", "section": "instructions", "type": "object", "description": "Files read from the session working directory and appended to the system prompt (AGENTS.md convention). The agent home's own AGENTS.md and DESIGN.md are read on top of this list whenever they exist, ahead of the project's pair, and are not named here. See https://coddy.dev/docs/features/rules."}
{"path": "instructions.files", "section": "instructions", "type": "array<string>", "description": "Instruction files, read in the order listed. ${CODDY_HOME}, ${CWD} and a leading ~ expand; an absolute entry is read as it stands, a relative one resolves against the session CWD.", "default": ["AGENTS.md", "DESIGN.md"]}
{"path": "logger", "section": "logger", "type": "object", "description": "Process log level, outputs, format, and file rotation."}
{"path": "logger.file", "section": "logger", "type": "string", "description": "Path for the file sink; required when outputs includes \"file\". Supports ${CODDY_HOME}."}
{"path": "logger.format", "section": "logger", "type": "string", "description": "Log record format.", "enum": ["text", "json"], "default": "text"}
{"path": "logger.level", "section": "logger", "type": "string", "description": "Minimum severity written to the configured outputs (\"warning\" is accepted as an alias of \"warn\").", "enum": ["debug", "info", "warn", "warning", "error"], "default": "info"}
{"path": "logger.levels", "section": "logger", "type": "array<object>", "description": "Per-component minimum severity. A component is the dotted name a record carries in its \"component\" attribute (gateway, gateway.telegram, session, agent, scheduler); a parent name covers everything nested under it and the longest configured prefix wins. Records with no component follow \"level\"."}
{"path": "logger.levels.[].component", "section": "logger", "type": "string", "description": "Dotted component name, for example \"gateway.telegram\".", "pattern": "^[a-z0-9_\\-]+(\\.[a-z0-9_\\-]+)*$"}
{"path": "logger.levels.[].level", "section": "logger", "type": "string", "description": "Minimum severity for this component.", "enum": ["debug", "info", "warn", "warning", "error"]}
{"path": "logger.outputs", "section": "logger", "type": "array<string>", "description": "Where log records go; any combination of stdout, stderr, file. Empty means stderr only.", "default": ["stderr"]}
{"path": "logger.rotation", "section": "logger", "type": "object", "description": "Size-based rotation for the file sink."}
{"path": "logger.rotation.max_files", "section": "logger", "type": "integer", "description": "Rotated backups to keep when max_size_mb > 0.", "default": 0, "minimum": 0}
{"path": "logger.rotation.max_size_mb", "section": "logger", "type": "integer", "description": "Rotate after the file reaches this size in MB; 0 disables size-based rotation.", "default": 0, "minimum": 0}
{"path": "mcp", "section": "mcp", "type": "object", "description": "MCP settings that are not tied to a single server entry."}
{"path": "mcp.project_trust", "section": "mcp", "type": "string", "description": "Trust policy for the project-local <cwd>/.coddy/mcp.json, which travels with the checkout: \"ask\" keeps its servers cold until the operator approves that exact declaration for that workspace; \"allow\" starts them automatically (trusted workspaces only); \"deny\" never loads them.", "enum": ["ask", "allow", "deny"], "default": "ask"}
{"path": "mcp_servers", "section": "mcp_servers", "type": "array<object>", "description": "Model Context Protocol servers connected for every new session."}
{"path": "mcp_servers.[].args", "section": "mcp_servers", "type": "array<string>", "description": "Argv passed after command for stdio servers. ${CWD} expands to the session cwd."}
{"path": "mcp_servers.[].command", "section": "mcp_servers", "type": "string", "description": "Executable for stdio transport (leave empty when using an http url). ${CWD} expands to the session cwd."}
{"path": "mcp_servers.[].disabled", "section": "mcp_servers", "type": "boolean", "description": "Skip connecting this server without removing its definition."}
{"path": "mcp_servers.[].disabled_tools", "section": "mcp_servers", "type": "array<string>", "description": "Tool names of this server hidden from the agent."}
{"path": "mcp_servers.[].env", "section": "mcp_servers", "type": "array<object>", "description": "Extra environment variables for the stdio child process."}
{"path": "mcp_servers.[].env.[].name", "section": "mcp_servers", "type": "string", "description": "Environment variable name."}
{"path": "mcp_servers.[].env.[].value", "section": "mcp_servers", "type": "string", "description": "Environment variable value. ${CWD} expands to the session cwd."}
{"path": "mcp_servers.[].headers", "section": "mcp_servers", "type": "array<object>", "description": "Optional headers sent with MCP HTTP requests. ${CWD} in a value expands to the session cwd."}
{"path": "mcp_servers.[].headers.[].name", "section": "mcp_servers", "type": "string", "description": "HTTP header name."}
{"path": "mcp_servers.[].headers.[].value", "section": "mcp_servers", "type": "string", "description": "HTTP header value."}
{"path": "mcp_servers.[].name", "section": "mcp_servers", "type": "string", "description": "Stable id referenced by the agent; must be unique in this list."}
{"path": "mcp_servers.[].type", "section": "mcp_servers", "type": "string", "description": "Transport. \"stdio\" (default when empty; url-only entries default to \"http\") runs a local command; \"http\" speaks streamable HTTP to url (with automatic legacy-SSE fallback); \"sse\" forces the legacy HTTP+SSE transport.", "enum": ["stdio", "http", "sse"]}
{"path": "mcp_servers.[].url", "section": "mcp_servers", "type": "string", "description": "HTTP(S) endpoint when type is \"http\". ${CWD} expands to the session cwd."}
{"path": "memory", "section": "memory", "type": "object", "description": "Optional memory copilot (implementation in external/memory; enable at runtime with memory.enable)."}
{"path": "memory.copilot_max_tokens", "section": "memory", "type": "integer", "description": "Completion token cap for memory copilot LLM calls.", "default": 4096}
{"path": "memory.dir", "section": "memory", "type": "string", "description": "Long-term memory root. Empty resolves to ${CODDY_HOME}/memory. Supports ${CODDY_HOME} and ~.", "default": ""}
{"path": "memory.enable", "section": "memory", "type": "boolean", "description": "Turn on the memory copilot.", "default": false}
{"path": "memory.max_search_hits", "section": "memory", "type": "integer", "description": "Maximum snippets returned by memory_search.", "default": 8}
{"path": "memory.model", "section": "memory", "type": "string", "description": "Exact models[].model id used only for recall/persist LLM calls; empty falls back to agent.model or the session override.", "default": ""}
{"path": "memory.persist_max_turns", "section": "memory", "type": "integer", "description": "Bounds persist-side LLM rounds in the memory loop.", "default": 12}
{"path": "memory.recall_max_turns", "section": "memory", "type": "integer", "description": "Bounds recall-side LLM rounds in the memory loop.", "default": 6}
{"path": "models", "section": "models", "type": "array<object>", "description": "Named model entries the agent and UI can select."}
{"path": "models.[].max_context_tokens", "section": "models", "type": "integer", "description": "Optional UI hint for the composer context bar; 0 derives it from provider metadata when available.", "default": 0}
{"path": "models.[].max_tokens", "section": "models", "type": "integer", "description": "Upper bound on completion tokens per assistant message. Ignored by Codex because its backend does not accept max_output_tokens."}
{"path": "models.[].model", "section": "models", "type": "string", "description": "\"provider_name/api_model_id\": the first path segment must match a providers[].name; the remainder is sent to the LLM API (may itself contain slashes).", "pattern": "^[^/]+/.+$"}
{"path": "models.[].multimodal", "section": "models", "type": "boolean", "description": "Model accepts image/file inputs in addition to text; the UI shows a file attachment button for this model.", "default": false}
{"path": "models.[].reasoning_default", "section": "models", "type": "string", "description": "Reasoning level pre-selected for new chats; must be one of the resolved levels, otherwise ignored."}
{"path": "models.[].reasoning_levels", "section": "models", "type": "array<string>", "description": "Override the reasoning levels offered for this model. Omit to auto-detect from the model id (gpt-5* -> minimal,low,medium,high; OpenAI o-series, gpt-oss*, qwen3*, and Claude extended-thinking models -> low,medium,high). An explicit empty list hides the selector. Settings fills this field from GET /coddy/config/reasoning-levels behind its Fetch reasoning levels button."}
{"path": "models.[].stream", "section": "models", "type": "boolean", "description": "Transport used to talk to this model. Omit (or true) to stream over SSE. false issues one blocking completion request and delivers the whole answer at once, for servers or proxies that handle event streams badly. Rejected for providers of type codex, whose backend is streaming-only.", "default": true}
{"path": "models.[].temperature", "section": "models", "type": "number", "description": "Sampling temperature (0 = deterministic; higher = more random)."}
{"path": "prompts", "section": "prompts", "type": "object", "description": "Override the built-in system prompt templates (Go text/template)."}
{"path": "prompts.agent_prompt", "section": "prompts", "type": "string", "description": "File name inside prompts.dir for agent mode.", "default": "agent.md"}
{"path": "prompts.ask_prompt", "section": "prompts", "type": "string", "description": "File name inside prompts.dir for ask mode.", "default": "ask.md"}
{"path": "prompts.dir", "section": "prompts", "type": "string", "description": "Directory containing prompt template files. Empty uses embedded defaults. Supports ~ and ${CWD} (session cwd at render time).", "default": ""}
{"path": "prompts.plan_prompt", "section": "prompts", "type": "string", "description": "File name inside prompts.dir for plan mode.", "default": "plan.md"}
{"path": "providers", "section": "providers", "type": "array<object>", "description": "API credentials and transport selection for upstream LLM vendors. When api_key is empty, the runtime reads the NAME_API_KEY environment variable (NAME is the provider name in uppercase with hyphens mapped to underscores)."}
{"path": "providers.[].api_base", "section": "providers", "type": "string", "description": "Optional base URL override. For type \"openai\" include /v1 (e.g. http://localhost:11434/v1); for type \"anthropic\" an Anthropic-compatible gateway (default https://api.anthropic.com). For type \"neuraldeep\" it selects the deployment: https://api.neuraldeep.ru/v1 (Russia, the default) or https://api.neuraldeep.tech/v1 (the international mirror); any other value falls back to the default. Ignored for type \"codex\", which always uses a fixed official endpoint."}
{"path": "providers.[].api_key", "section": "providers", "type": "string", "description": "Provider secret. A literal key, a \"${ENV}\" reference expanded at load time, or empty to read NAME_API_KEY at LLM call time. Resolution order: api_key -> api_key_command stdout -> NAME_API_KEY."}
{"path": "providers.[].api_key_command", "section": "providers", "type": "string", "description": "Optional credential-helper command. When api_key is empty it runs via the detected host shell (pwsh, powershell, or cmd on Windows; bash or sh elsewhere) and the trimmed stdout is used as the key. On failure resolution falls back to NAME_API_KEY."}
{"path": "providers.[].name", "section": "providers", "type": "string", "description": "Logical id used as the first segment of models[].model. ASCII letters, digits, hyphen, underscore; must start with a letter.", "pattern": "^[a-zA-Z][a-zA-Z0-9_\\-]*$"}
{"path": "providers.[].proxy", "section": "providers", "type": "string", "description": "Optional per-provider outbound proxy URL: http://, https://, socks5://, or socks5h:// (socks5h resolves hostnames via the proxy)."}
{"path": "providers.[].timeout_ms", "section": "providers", "type": "integer", "description": "Optional bound on each LLM HTTP request to this provider, including the streamed body read. 0 (the default) sets no client timeout, so slow prompt processing on large contexts is never cut short.", "default": 0, "minimum": 0}
{"path": "providers.[].type", "section": "providers", "type": "string", "description": "Wire protocol for this provider. Use \"openai\" for configurable OpenAI-compatible endpoints (OpenAI, DeepSeek, Groq, Ollama, llama.cpp, LM Studio), \"anthropic\" for Anthropic, \"neuraldeep\" for NeuralDeep's OpenAI-compatible API at one of its two official deployments (selected with api_base), or \"codex\" for ChatGPT OAuth against the official Codex backend (Responses API).", "enum": ["openai", "anthropic", "neuraldeep", "codex"]}
{"path": "providers.[].usage_limits_panel", "section": "providers", "type": "boolean", "description": "Show this provider's account usage panel (the console footer line and /usage, the usage section and banner in the web UI) and read the provider's usage endpoint for it (GET /v1/limits for type neuraldeep). Omit or true keeps the panel on; false hides it and stops those reads for this row. Only providers whose type has a usage source are affected.", "default": true}
{"path": "rules", "section": "rules", "type": "object", "description": "Discovery of rule files from ${CODDY_HOME}/rules (the operator's own, applied in every workspace) and from .coddy/rules, .agents/rules, .cursor/rules, .claude/rules, .codex/rules under the session CWD, plus the AGENTS.md and DESIGN.md of a folder a tool enters; .mdc files are Cursor rules, .md files Claude Code rules. See https://coddy.dev/docs/features/rules."}
{"path": "rules.auto_discover", "section": "rules", "type": "boolean", "description": "Scan the session CWD rule roots automatically.", "default": true}
{"path": "rules.systems", "section": "rules", "type": "array<string>", "description": "Restrict which rule systems are loaded: user (${CODDY_HOME}/rules), coddy, agents-dir (.agents/rules), cursor, claude, codex, agents (nested AGENTS.md and DESIGN.md). Empty means all.", "default": []}
{"path": "scheduler", "section": "scheduler", "type": "object", "description": "Cron-driven scheduled jobs (used only by binaries built with -tags scheduler). Jobs are flat *.md files with YAML frontmatter under scheduler.dir; five-field crontab in UTC."}
{"path": "scheduler.dir", "section": "scheduler", "type": "string", "description": "Directory with *.md job definitions. Empty resolves to ${CODDY_HOME}/scheduler.", "default": ""}
{"path": "scheduler.enable", "section": "scheduler", "type": "boolean", "description": "Run the scheduler daemon and expose the coddy_scheduler_* tools. Can also be forced per process with coddy serve --scheduler (or coddy acp -scheduler).", "default": false}
{"path": "scheduler.max_queue", "section": "scheduler", "type": "integer", "description": "Concurrent scheduled runs; when saturated, extra firings are skipped until a slot frees.", "default": 10}
{"path": "scheduler.retain_sessions", "section": "scheduler", "type": "integer", "description": "Completed scheduler-run session directories kept per job_id under sessions.dir; older runs are pruned.", "default": 5}
{"path": "scheduler.timeout", "section": "scheduler", "type": "string", "description": "Wall-clock limit for one scheduled agent run, as a Go duration (e.g. \"30m\", \"1h30m\").", "default": "30m"}
{"path": "sessions", "section": "sessions", "type": "object", "description": "Where persisted session bundles are stored."}
{"path": "sessions.dir", "section": "sessions", "type": "string", "description": "Sessions root directory. Empty resolves to ${CODDY_HOME}/sessions. Supports ${CODDY_HOME} and ~.", "default": ""}
{"path": "skills", "section": "skills", "type": "object", "description": "Directories scanned for skills (SKILL.md and root .md/.mdc files)."}
{"path": "skills.auto_discovery", "section": "skills", "type": "boolean", "description": "Offer the model-driven load_skill tool so the agent pulls a catalogued skill's full instructions into a turn on its own when the request matches, instead of requiring an explicit /name. Defaults to true.", "default": true}
{"path": "skills.dirs", "section": "skills", "type": "array<string>", "description": "Search paths; later entries win on name conflicts. Defaults (lowest to highest priority): ~/.agents/skills, ${CODDY_HOME}/skills, ${CWD}/.coddy/skills. ${CODDY_HOME} expands when the file is loaded; ${CWD} stays in the entry and expands per session against that session's workspace.", "default": ["~/.agents/skills", "${CODDY_HOME}/skills", "${CWD}/.coddy/skills"]}
{"path": "skills.sources", "section": "skills", "type": "array<string>", "description": "Remote skill sources installed on demand with `coddy skills sync` (never fetched automatically). Each entry is a GitHub repo (owner/repo[@ref]), a git URL, or an http(s) URL to an agents-standard marketplace.json. Materialized into ${CODDY_HOME}/skills."}
{"path": "subagents", "section": "subagents", "type": "object", "description": "User-defined child agents the model can delegate to with spawn_agent. Definitions are markdown files with YAML frontmatter; each run is a background task of the parent session with its own child session and transcript. See https://coddy.dev/docs/features/subagents."}
{"path": "subagents.default_timeout_seconds", "section": "subagents", "type": "integer", "description": "Hard limit for one run whose definition and call give no timeout. Capped by tools.background.max_timeout_seconds. 0 uses the default.", "default": 1800}
{"path": "subagents.dirs", "section": "subagents", "type": "array<string>", "description": "Definition directories, lowest priority first; later entries override earlier ones by name. ${CODDY_HOME} and ${CWD} expand. Directories inside the workspace are project scope and follow project_trust.", "default": ["${CODDY_HOME}/agents", "${CWD}/.claude/agents", "${CWD}/.coddy/agents"]}
{"path": "subagents.enable", "section": "subagents", "type": "boolean", "description": "Register the spawn_agent tool and list the subagent catalog in the system prompt.", "default": true}
{"path": "subagents.max_concurrent", "section": "subagents", "type": "integer", "description": "How many subagent runs the whole process may have in flight at once, whatever session started them. Starting past the limit is refused, not queued. 0 uses the default.", "default": 4}
{"path": "subagents.max_depth", "section": "subagents", "type": "integer", "description": "How deep spawning may nest: 1 lets a session spawn subagents that cannot spawn further; 0 forbids spawning everywhere. Omit for the default.", "default": 1}
{"path": "subagents.max_turns", "section": "subagents", "type": "integer", "description": "ReAct rounds a child may take. 0 follows agent.max_turns.", "default": 0}
{"path": "subagents.project_trust", "section": "subagents", "type": "string", "description": "Trust policy for definitions found inside the workspace, which travel with the checkout: \"ask\" loads them but refuses to spawn one until the operator approved that exact file for that workspace on the machine running coddy (coddy agents trust there, or POST /coddy/subagents/{name}/trust with the session workspace as cwd); \"allow\" treats them like the operator's own files; \"deny\" never reads them.", "enum": ["ask", "allow", "deny"], "default": "ask"}
{"path": "swarm", "section": "swarm", "type": "object", "description": "Stateless relay that nodes register into and that chains into other relays. The relay itself needs -tags swarm and swarm.enable; the join list below is honoured by any coddy serve process, which is what makes an ordinary agent reachable through a relay. See https://coddy.dev/docs/operate/swarm."}
{"path": "swarm.allow_insecure", "section": "swarm", "type": "boolean", "description": "Permit binding off loopback without a client token.", "default": false}
{"path": "swarm.allow_private_upstreams", "section": "swarm", "type": "array<string>", "description": "Hosts a node may advertise even though they resolve into loopback or private ranges, which are otherwise refused so a registration cannot turn the relay into a probe of its own network.", "default": []}
{"path": "swarm.auth_token", "section": "swarm", "type": "string", "description": "Bearer credential clients present to this relay. A relay reaches every node with that node's own credential, so an unauthenticated relay bound off loopback is a fleet-wide open door and the server refuses to start unless allow_insecure is set. Never echoed back.", "default": ""}
{"path": "swarm.cors", "section": "swarm", "type": "object", "description": "Cross-origin access for the relay API. The SPA reaches a relay from another origin by construction, so this usually has to be on. Allow-Headers includes Last-Event-ID so an SSE stream can be resumed through the relay."}
{"path": "swarm.cors.allowed_origins", "section": "swarm", "type": "array<string>", "description": "Exact origins permitted to call the API, e.g. \"http://localhost:5173\". A single \"*\" allows any origin.", "default": []}
{"path": "swarm.cors.enable", "section": "swarm", "type": "boolean", "description": "Handle CORS preflight and emit Access-Control-* headers for allowed origins.", "default": false}
{"path": "swarm.enable", "section": "swarm", "type": "boolean", "description": "Run the swarm relay in this process. Independent of swarm.join, which is how an agent registers into a parent relay.", "default": false}
{"path": "swarm.fanout_timeout_seconds", "section": "swarm", "type": "integer", "description": "Per-node deadline for an aggregated call. A slower node degrades into a warning rather than stalling the answer. 0 falls back to 3.", "default": 0, "minimum": 0}
{"path": "swarm.host", "section": "swarm", "type": "string", "description": "Bind address for the relay when the CLI does not pass --swarm-host. Empty falls back to 0.0.0.0.", "default": ""}
{"path": "swarm.insecure_open_registration", "section": "swarm", "type": "boolean", "description": "Let any caller register a node without a pairing token. Development only.", "default": false}
{"path": "swarm.join", "section": "swarm", "type": "array<object>", "description": "Parent relays this process registers into on startup. Honoured whether or not this process runs a relay of its own, which is what lets relays chain.", "default": []}
{"path": "swarm.join.[].advertise_url", "section": "swarm", "type": "string", "description": "Where the relay can reach this process. Leave empty to select the tunnel transport: this process dials out and serves its API back over that connection, which is the only way in when the network accepts no inbound connections.", "default": ""}
{"path": "swarm.join.[].dial", "section": "swarm", "type": "object", "description": "How this leg reaches the other side. Relays are expected to live in different networks, so a proxy hop and a private certificate authority are ordinary here."}
{"path": "swarm.join.[].dial.ca_file", "section": "swarm", "type": "string", "description": "Certificate authority used to verify a peer whose certificate is signed privately.", "default": ""}
{"path": "swarm.join.[].dial.insecure_skip_verify", "section": "swarm", "type": "boolean", "description": "Accept any certificate. For a lab only; every use is logged.", "default": false}
{"path": "swarm.join.[].dial.proxy", "section": "swarm", "type": "string", "description": "Route the connection through a proxy: http, https, socks5 or socks5h. Empty falls back to the standard environment variables. Never echoed back.", "default": ""}
{"path": "swarm.join.[].labels", "section": "swarm", "type": "object", "description": "Free-form tags shown in topology views."}
{"path": "swarm.join.[].name", "section": "swarm", "type": "string", "description": "Name to claim in that relay. Empty falls back to the host name.", "default": ""}
{"path": "swarm.join.[].pairing_token", "section": "swarm", "type": "string", "description": "Credential authorising the registration. Never echoed back.", "default": ""}
{"path": "swarm.join.[].token", "section": "swarm", "type": "string", "description": "This process's own bearer credential, handed to the relay so it can authenticate when it proxies. Prefer a credential minted for the relay alone. Never echoed back.", "default": ""}
{"path": "swarm.join.[].url", "section": "swarm", "type": "string", "description": "Parent relay origin."}
{"path": "swarm.lease_ttl_seconds", "section": "swarm", "type": "integer", "description": "How long a registration survives without a heartbeat. 0 falls back to 90; nodes refresh at a third of it.", "default": 0, "minimum": 0}
{"path": "swarm.name", "section": "swarm", "type": "string", "description": "Label for this relay in topology views and in a child's node path.", "default": ""}
{"path": "swarm.pairing_tokens", "section": "swarm", "type": "array<string>", "description": "Credentials a node must present to register. Empty closes registration unless insecure_open_registration is set. Never echoed back.", "default": []}
{"path": "swarm.port", "section": "swarm", "type": "integer", "description": "Listen port for the relay. 0 falls back to 12346.", "default": 0, "minimum": 0, "maximum": 65535}
{"path": "swarm.tls", "section": "swarm", "type": "object", "description": "Serve the relay over HTTPS. Set both files or neither. Minimum TLS 1.2; certificates are startup state, so rotating them needs a restart."}
{"path": "swarm.tls.cert_file", "section": "swarm", "type": "string", "description": "PEM certificate chain.", "default": ""}
{"path": "swarm.tls.key_file", "section": "swarm", "type": "string", "description": "PEM private key.", "default": ""}
{"path": "swarm.upstreams", "section": "swarm", "type": "array<object>", "description": "Nodes configured by hand rather than registered, for an agent that cannot run the join loop.", "default": []}
{"path": "swarm.upstreams.[].dial", "section": "swarm", "type": "object", "description": "How this leg reaches the other side. Relays are expected to live in different networks, so a proxy hop and a private certificate authority are ordinary here."}
{"path": "swarm.upstreams.[].dial.ca_file", "section": "swarm", "type": "string", "description": "Certificate authority used to verify a peer whose certificate is signed privately.", "default": ""}
{"path": "swarm.upstreams.[].dial.insecure_skip_verify", "section": "swarm", "type": "boolean", "description": "Accept any certificate. For a lab only; every use is logged.", "default": false}
{"path": "swarm.upstreams.[].dial.proxy", "section": "swarm", "type": "string", "description": "Route the connection through a proxy: http, https, socks5 or socks5h. Empty falls back to the standard environment variables. Never echoed back.", "default": ""}
{"path": "swarm.upstreams.[].kind", "section": "swarm", "type": "string", "description": "agent (default) or relay.", "enum": ["", "agent", "relay"], "default": ""}
{"path": "swarm.upstreams.[].name", "section": "swarm", "type": "string", "description": "Node name; becomes a URL path segment, so letters, digits, underscore and hyphen only."}
{"path": "swarm.upstreams.[].token", "section": "swarm", "type": "string", "description": "Bearer credential the relay presents to this node. Never echoed back.", "default": ""}
{"path": "swarm.upstreams.[].url", "section": "swarm", "type": "string", "description": "Origin the relay dials to reach the node."}
{"path": "tools", "section": "tools", "type": "object", "description": "Filesystem and shell policy for built-in tools."}
{"path": "tools.background", "section": "tools", "type": "object", "description": "Commands the agent runs detached in the session task pool instead of blocking a turn. The pool dies with the coddy process; each task's metadata and captured output stay in the session bundle, and a task interrupted by a restart is reported as orphaned."}
{"path": "tools.background.default_timeout_seconds", "section": "tools", "type": "integer", "description": "Hard limit for a task started without an explicit timeout and without a duration estimate. 0 uses the default.", "default": 900}
{"path": "tools.background.enable", "section": "tools", "type": "boolean", "description": "Offer the background option on run_command and expose the background task tools.", "default": true}
{"path": "tools.background.max_concurrent", "section": "tools", "type": "integer", "description": "How many background tasks one session may run at once. 0 uses the default.", "default": 5}
{"path": "tools.background.max_timeout_seconds", "section": "tools", "type": "integer", "description": "Ceiling applied to any requested or estimate-derived timeout. 0 uses the default.", "default": 3600}
{"path": "tools.background.output_buffer_bytes", "section": "tools", "type": "integer", "description": "How much of each task's output stays in memory for the status ticker. The full log is still written to the session bundle. 0 uses the default.", "default": 262144}
{"path": "tools.command_allowlist", "section": "tools", "type": "array<string>", "description": "Commands that never require permission. Exact or prefix match (prefix + space + any args). \"*\" allows all commands."}
{"path": "tools.output_limits", "section": "tools", "type": "object", "description": "Maximum lines each tool result or error may return into the LLM context. Every enabled limit also applies a 64 KiB per-call byte safety ceiling so a huge single line cannot bypass it. 0 disables both limits for that tool. Unset fields fall back to the built-in defaults."}
{"path": "tools.output_limits.default", "section": "tools", "type": "integer", "description": "Applies to any tool not named above, including MCP tools. 0 means unlimited.", "default": 1000, "minimum": 0}
{"path": "tools.output_limits.glob", "section": "tools", "type": "integer", "description": "Max paths returned by glob.", "default": 300, "minimum": 0}
{"path": "tools.output_limits.grep", "section": "tools", "type": "integer", "description": "Max path:line:content records returned by grep.", "default": 200, "minimum": 0}
{"path": "tools.output_limits.print_tree", "section": "tools", "type": "integer", "description": "Max lines of a directory tree.", "default": 400, "minimum": 0}
{"path": "tools.output_limits.read", "section": "tools", "type": "integer", "description": "Max lines for a read file page or directory listing.", "default": 1000, "minimum": 0}
{"path": "tools.output_limits.run_command", "section": "tools", "type": "integer", "description": "Max combined stdout+stderr lines of a shell command.", "default": 500, "minimum": 0}
{"path": "tools.output_limits.ssh_run_command", "section": "tools", "type": "integer", "description": "Max combined stdout+stderr lines of a remote SSH command.", "default": 500, "minimum": 0}
{"path": "tools.output_limits.webfetch", "section": "tools", "type": "integer", "description": "Max lines of fetched page markdown.", "default": 800, "minimum": 0}
{"path": "tools.output_limits.websearch", "section": "tools", "type": "integer", "description": "Max lines of search results.", "default": 200, "minimum": 0}
{"path": "tools.permission_mode", "section": "tools", "type": "string", "description": "When the agent asks for user approval: \"ask\" prompts for commands and file writes; \"accept_edits\" auto-approves writes but prompts for commands; \"bypass\" never asks (trusted environments only).", "enum": ["ask", "accept_edits", "bypass"], "default": "ask"}
{"path": "tools.ssh_connect_timeout", "section": "tools", "type": "integer", "description": "TCP dial timeout for SSH connections (ssh_run_command tool), in seconds.", "default": 30}
{"path": "ui", "section": "ui", "type": "object", "description": "Controls the bundled single-page UI (only meaningful in binaries built with -tags http,ui)."}
{"path": "ui.enable", "section": "ui", "type": "boolean", "description": "Serve the embedded SPA at GET /. Defaults to true; set false to run an API-only server (the API still requires httpserver.auth_token when configured).", "default": true}
